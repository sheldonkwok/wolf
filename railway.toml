[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

# Attach a persistent volume at /data in Railway; volumes are not provisioned by this file.
[deploy]
numReplicas = 1
sleepApplication = false
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
overlapSeconds = 0
drainingSeconds = 10
